import React from "react";

const Footer = () => {
    return(
	<nav className="navbar theme-navbar">
		{/* currenty using the same code as of the navbar but can change the footer as per requirement */}
	      <ul className="theme-nav footer">
	        <li><a href="#about">Footer goes here</a></li>
	      </ul>
	</nav>
    )
}

export default Footer